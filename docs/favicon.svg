<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <!-- The product mark (.bn-mark in bento-components.css): an orange dot,
       set on an ink tile so it holds on both a light and a dark browser tab.
       No name in here on purpose, so renaming the brand never touches this
       file. -->
  <rect width="64" height="64" rx="14" fill="#121213"/>
  <circle cx="32" cy="32" r="15" fill="#FF6022"/>
</svg>
